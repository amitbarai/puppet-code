---
# File should be present in /etc/puppetlabs/puppet and can be deployed using below command 
# /opt/puppetlabs/puppet/bin/r10k deploy environment -pv -c /etc/puppetlabs/code/environments/production/r10k.yaml
# Default path is /etc/puppet/r10k.yaml         
# note, you have to create this file if it doesn't already exist.
:cachedir: '/app/r10k/cache'
:sources:
  :local:
    remote: 'https://github.com/amitbarai/puppet-code.git'
    basedir: /etc/puppetlabs/code/environments