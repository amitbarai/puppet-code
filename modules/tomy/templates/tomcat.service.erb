# Systemd unit file for tomcat instances.
#
# To create clones of this service:
# 0. systemctl enable tomcat@name.service
# 1. create catalina.base directory structure in
#    /var/lib/tomcats/name
# 2. profit.

[Unit]
Description=Apache tomcat <%= @tomcatservicename %> Web Application Container
After=syslog.target network.target

[Service]
Type=simple
EnvironmentFile=<%= @catalina_home %>/conf/tomcat.conf
Environment="NAME="
EnvironmentFile=-/etc/sysconfig/tomcat
ExecStart=/etc/systemd/system/<%= @tomcatservicename %>.service start
ExecStop=/etc/systemd/system/<%= @tomcatservicename %>.service stop
SuccessExitStatus=143
User=tomcat
Group=tomcat

[Install]
WantedBy=multi-user.target
